<div class="container p-4">
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-md-12 mb-5">
                    <h3>Lista de clientes</h3>
                </div>
                <div class="col-md-12">
                    <table class="data-table table table-bordered">
                        <thead>
                            <tr>
                                <th>Código</th>
                                <th>Nombre</th>
                                <th>Creado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each customers}}
                            <tr>
                                <td>{{code}}</td>
                                <td>{{name}}</td>
                                <td>{{timeago created_at}}</td>
                                <td><a href="/customers/locations/{{id}}" class="btn btn-primary btn-sm" target="_blank"> <i class="fa fa-map-marker"></i> Ubicaciones</a></td>
                            </tr>
                            {{else}}
                            <tr>
                                <td colspan="4">No hay registros</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>